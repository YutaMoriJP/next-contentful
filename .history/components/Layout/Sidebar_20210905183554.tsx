interface NavOption {
  name: string;
  path: string;
  id: string;
}
interface SidebarProps {
  nav: NavOption[];
}
const Sidebar = ({nav}SidebarProps) => {
  return <></>;
};

export default Sidebar;
